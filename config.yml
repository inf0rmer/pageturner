development:
  redis:
    url: <%= ENV["REDIS_URL"] %>
  rabbitmq:
    url: <%= ENV["RABBITMQ_URL"] %>

test:
  redis:
    url: <%= ENV["REDIS_URL"] %>
  rabbitmq:
    url: <%= ENV["RABBITMQ_URL"] %>

production:
  redis:
    url: <%= ENV["REDIS_URL"] %>
  rabbitmq:
    url: <%= ENV["RABBITMQ_URL"] %>
