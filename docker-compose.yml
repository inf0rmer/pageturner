version: "2"

services:
  app:
    build: .
    ports:
      - 80
    links:
      - redis
      - rabbitmq
    env_file:
      - .env
    environment:
      APP_ENV: development

  redis:
    image: redis:3.2

  rabbitmq:
    image: rabbitmq:3.6
