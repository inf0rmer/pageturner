version: "2"

services:
  app:
    build: .
    links:
      - hivent
    environment:
      APP_ENV:
      MONITORED_BRANCH:
      ROLLBAR_ACCESS_TOKEN:
      S3_BUCKET_NAME:
      AWS_ACCESS_KEY_ID:
      AWS_SECRET_KEY:
      AWS_REGION:
      HIVENT_URL: redis://hivent:6379
      HIVENT_PARTITION_COUNT: 4

  hivent:
    image: redis:3.2
